## 2.0.0

- [!tag|A|0|] 新增 `fullKeyboard` 表单项类型，支持全键盘输入。
- [!tag|A|0|] 新增 `colorPicker` 表单项类型，支持颜色选择器。
- [!tag|A|0|] Picker 多选模式返回值新增 `multipleSelected`。
- [!tag|O|0|] `card` 从 boolean 升级为 `CardProps`，并移除 `mx`、`px`、`radius`、`shadow` 配置。

