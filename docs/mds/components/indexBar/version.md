## 2.0.0

- [!tag|A|0|] 新增 `children` Snippet，支持自定义子项渲染。
- [!tag|A|0|] `data` 与 `onclickChild` 支持泛型 `IndexBarItemProps<T>`，可传入复杂数据结构。
- [!tag|O|0|] `radius` 选项扩展并调整默认值。

## 0.5.1

- [!tag|O|0|]优化滑动动画效果。
