## 2.0.0

- [!tag|A|0|] 新增 `radius` 属性，支持自定义圆角风格。
- [!tag|O|0|] 滚动区域高度与对齐方式优化，显示更稳定。

## 1.1.1

- [!tag|O|0|] 去除 `fontSize` 属性，不再支持设置字体大小。

## 1.1.0

- [!tag|O|0|] `leftIcon` 属性新增 `volume` 选项并默认值为 `volume`。
